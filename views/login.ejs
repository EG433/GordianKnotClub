<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Arial', sans-serif;
        background-color: yellow;
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        position: relative;
    }

    .container {
        background: white;
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        width: 100%;
        max-width: 400px;
        position: relative;
    }

    .home-btn {
        position: absolute;
        top: -200px;
        right: -200px;
        padding: 10px;
        background-color: #6c757d;
        color: #ffffff;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    }

    .home-btn:hover {
        background-color: #5a6268;
    }

    .form {
        display: flex;
        flex-direction: column;
    }

    .heading {
        font-size: 24px;
        color: #333;
        margin-bottom: 20px;
        text-align: center;
    }

    input[type="email"], input[type="password"] {
        padding: 10px;
        margin-bottom: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        width: 100%;
    }

    input[type="email"]:focus, input[type="password"]:focus {
        border-color: #0056b3;
        outline: none;
    }

    .submit-btn {
        background-color: #007bff;
        color: white;
        padding: 10px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        margin-top: 10px;
    }

    .submit-btn:hover {
        background-color: #0056b3;
    }

    .link-container {
        margin-top: 20px;
        text-align: center;
    }

    .link {
        color: #007bff;
        text-decoration: none;
    }

    .link:hover {
        text-decoration: underline;
    }

    .alert-box {
        background-color: #f8d7da;
        color: #721c24;
        padding: 10px;
        margin-bottom: 20px;
        border-radius: 4px;
        text-align: center;
    }

  </style>
</head>
<body>
    <div class="container">
        <div class="form">
            <% if (messages && messages.error) { %>
                <div class="alert-box">
                    <strong><%= messages.error %></strong>
                </div>
            <% } %>
            <h1 class="heading">Login</h1>
            <form action="/login" method="POST">
                <!-- Email input -->
                <input type="email" name="email" placeholder="Email address" required>
                <!-- Password input -->
                <input type="password" name="password" placeholder="Password" required>
                <!-- Submit button -->
                <button type="submit" class="submit-btn">Sign In</button>
                <!-- Register link -->
                <div class="link-container">
                    <a href="/register" class="link">Don't have an account yet? Register</a>
                </div>
                <div class="button-container">
                    <button type="button" class="home-btn" onclick="window.location.href='/home'">Home Page</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>

